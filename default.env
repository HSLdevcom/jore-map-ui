# Rename to `.env` to use.
# These will not be used if an environment variable by the same name exists.
# Only variables named `JORE_UI_*` or ones defined in `env.js` are exported to frontend application by default.

# Only variables named CYPRESS_* will be exported to cypress
CYPRESS_HSLID_READ_ACCESS_USERNAME = <fill>
CYPRESS_HSLID_READ_ACCESS_PASSWORD = <fill>
CYPRESS_HSLID_WRITE_ACCESS_USERNAME = <fill>
CYPRESS_HSLID_WRITE_ACCESS_PASSWORD = <fill>
CYPRESS_HSLID_CLIENT_ID = <fill>
CYPRESS_HSLID_CLIENT_SECRET = <fill>