enum SubSites{
    home = '/',
    routes = '/routes/',
    node = '/node/:id',
    link = '/link/:id',
    routePath = '/routePath/:id',
    newRoutePath = '/routePath/new',
    network = '/network/',
    current = '',
}

export default SubSites;
