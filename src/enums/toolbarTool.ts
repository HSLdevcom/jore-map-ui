enum ToolbarTool {
    AddNetworkNode,
    AddNewRoutePathLink,
    Copy,
    DivideLink,
    RemoveRoutePathLink,
    EditNetworkNode,
    Print,
    SelectNetworkEntity,
}

export default ToolbarTool;
